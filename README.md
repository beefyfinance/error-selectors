# error-decode-example

To install dependencies:

```bash
bun install
```

To run example:

```bash
bun run start
```

To run tests:

```bash
bun test
```

To add more errors:

```bash
bun run add path/to/abi.json
```